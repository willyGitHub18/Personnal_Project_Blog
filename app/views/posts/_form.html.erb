<%= form_for @post do |f| %>

    <%= render 'shared/error_messages' %>

    <div class="field">
        <%= f.collection_select(:category_id, Category.all, :id, :name, { prompt: "Choisir une catégorie" } ) %><br />
    </div>
    <div class="field">
        <%= f.label "Titre", class: "label-field" %><br />
        <%= f.text_field :title, placeholder: "Saisir le titre de l'article" %><br />
    </div>
    <div class="area">
        <%= f.label "Contenu", class: "label-field" %><br />
        <%= f.text_area :content, placeholder: "Contenu de l'article" %><br />
    </div>
    <div class="actions">
        <%= f.submit "Poster", class: "btn btn-primary" %>
    </div>

<% end %>
